app:
  name: My App
  description: Welcome to ${app.name}

my:
  greeting: Hello New
  list:
    values: One,Two,Three
dbValues: "{connectionString: 'test-connection-string-here', userName: 'foo', password: 'pass'}"

db:
  connection: test-connection-string-here
  host: 127.0.0.1
  port: 2400

management:
  endpoints:
    web:
      exposure:
        include: "*"